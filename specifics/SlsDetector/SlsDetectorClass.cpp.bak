/*----- PROTECTED REGION ID(SlsDetectorClass.cpp) ENABLED START -----*/
static const char *RcsId      = "$Id:  $";
static const char *TagName    = "$Name:  $";
static const char *CvsPath    = "$Source:  $";
static const char *SvnPath    = "$HeadURL:  $";
static const char *HttpServer = "http://www.esrf.eu/computing/cs/tango/tango_doc/ds_doc/";
//=============================================================================
//
// file :        SlsDetectorClass.cpp
//
// description : C++ source for the SlsDetectorClass.
//               A singleton class derived from DeviceClass.
//               It implements the command and attribute list
//               and all properties and methods required
//               by the SlsDetector once per process.
//
// project :     Sls dectector TANGO specific device.
//
// This file is part of Tango device class.
// 
// Tango is free software: you can redistribute it and/or modify
// it under the terms of the GNU General Public License as published by
// the Free Software Foundation, either version 3 of the License, or
// (at your option) any later version.
// 
// Tango is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU General Public License for more details.
// 
// You should have received a copy of the GNU General Public License
// along with Tango.  If not, see <http://www.gnu.org/licenses/>.
// 
// $Author:  $
//
// $Revision:  $
// $Date:  $
//
// $HeadURL:  $
//
//=============================================================================
//                This file is generated by POGO
//        (Program Obviously used to Generate tango Object)
//=============================================================================


#include <SlsDetectorClass.h>

/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass.cpp

//-------------------------------------------------------------------
/**
 *	Create SlsDetectorClass singleton and
 *	return it in a C function for Python usage
 */
//-------------------------------------------------------------------
extern "C" {
#ifdef _TG_WINDOWS_

__declspec(dllexport)

#endif

	Tango::DeviceClass *_create_SlsDetector_class(const char *name) {
		return SlsDetector_ns::SlsDetectorClass::init(name);
	}
}

namespace SlsDetector_ns
{
//===================================================================
//	Initialize pointer for singleton pattern
//===================================================================
SlsDetectorClass *SlsDetectorClass::_instance = NULL;

//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::SlsDetectorClass(string &s)
 * description : 	constructor for the SlsDetectorClass
 *
 * @param s	The class name
 */
//--------------------------------------------------------
SlsDetectorClass::SlsDetectorClass(string &s):Tango::DeviceClass(s)
{
	cout2 << "Entering SlsDetectorClass constructor" << endl;
	set_default_property();
	write_class_property();

	/*----- PROTECTED REGION ID(SlsDetectorClass::constructor) ENABLED START -----*/
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::constructor

	cout2 << "Leaving SlsDetectorClass constructor" << endl;
}

//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::~SlsDetectorClass()
 * description : 	destructor for the SlsDetectorClass
 */
//--------------------------------------------------------
SlsDetectorClass::~SlsDetectorClass()
{
	/*----- PROTECTED REGION ID(SlsDetectorClass::destructor) ENABLED START -----*/
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::destructor

	_instance = NULL;
}


//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::init
 * description : 	Create the object if not already done.
 *                  Otherwise, just return a pointer to the object
 *
 * @param	name	The class name
 */
//--------------------------------------------------------
SlsDetectorClass *SlsDetectorClass::init(const char *name)
{
	if (_instance == NULL)
	{
		try
		{
			string s(name);
			_instance = new SlsDetectorClass(s);
		}
		catch (bad_alloc &)
		{
			throw;
		}		
	}		
	return _instance;
}

//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::instance
 * description : 	Check if object already created,
 *                  and return a pointer to the object
 */
//--------------------------------------------------------
SlsDetectorClass *SlsDetectorClass::instance()
{
	if (_instance == NULL)
	{
		cerr << "Class is not initialised !!" << endl;
		exit(-1);
	}
	return _instance;
}



//===================================================================
//	Command execution method calls
//===================================================================
//--------------------------------------------------------
/**
 * method : 		SetCmdClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *SetCmdClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "SetCmdClass::execute(): arrived" << endl;
	Tango::DevString argin;
	extract(in_any, argin);
	((static_cast<SlsDetector *>(device))->set_cmd(argin));
	return new CORBA::Any();
}

//--------------------------------------------------------
/**
 * method : 		GetCmdClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *GetCmdClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "GetCmdClass::execute(): arrived" << endl;
	Tango::DevString argin;
	extract(in_any, argin);
	return insert((static_cast<SlsDetector *>(device))->get_cmd(argin));
}


//===================================================================
//	Properties management
//===================================================================
//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::get_class_property()
 *	Description : Get the class property for specified name.
 */
//--------------------------------------------------------
Tango::DbDatum SlsDetectorClass::get_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_prop.size() ; i++)
		if (cl_prop[i].name == prop_name)
			return cl_prop[i];
	//	if not found, returns  an empty DbDatum
	return Tango::DbDatum(prop_name);
}

//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::get_default_device_property()
 *	Description : Return the default value for device property.
 */
//--------------------------------------------------------
Tango::DbDatum SlsDetectorClass::get_default_device_property(string &prop_name)
{
	for (unsigned int i=0 ; i<dev_def_prop.size() ; i++)
		if (dev_def_prop[i].name == prop_name)
			return dev_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}

//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::get_default_class_property()
 *	Description : Return the default value for class property.
 */
//--------------------------------------------------------
Tango::DbDatum SlsDetectorClass::get_default_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_def_prop.size() ; i++)
		if (cl_def_prop[i].name == prop_name)
			return cl_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}


//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::set_default_property()
 *	Description : Set default property (class and device) for wizard.
 *                For each property, add to wizard property name and description.
 *                If default value has been set, add it to wizard property and
 *                store it in a DbDatum.
 */
//--------------------------------------------------------
void SlsDetectorClass::set_default_property()
{
	string	prop_name;
	string	prop_desc;
	string	prop_def;
	vector<string>	vect_data;

	//	Set Default Class Properties

	//	Set Default device Properties
	prop_name = "ConfigFileName";
	prop_desc = "Configuration files used during the camera instanciation.";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
}

//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::write_class_property()
 *	Description : Set class description fields as property in database
 */
//--------------------------------------------------------
void SlsDetectorClass::write_class_property()
{
	//	First time, check if database used
	if (Tango::Util::_UseDb == false)
		return;

	Tango::DbData	data;
	string	classname = get_name();
	string	header;
	string::size_type	start, end;

	//	Put title
	Tango::DbDatum	title("ProjectTitle");
	string	str_title("Sls dectector TANGO specific device.");
	title << str_title;
	data.push_back(title);

	//	Put Description
	Tango::DbDatum	description("Description");
	vector<string>	str_desc;
	str_desc.push_back("Device for sls detectors from PSI. ");
	description << str_desc;
	data.push_back(description);

	//	put cvs or svn location
	string	filename("SlsDetector");
	filename += "Class.cpp";

	// check for cvs information
	string	src_path(CvsPath);
	start = src_path.find("/");
	if (start!=string::npos)
	{
		end   = src_path.find(filename);
		if (end>start)
		{
			string	strloc = src_path.substr(start, end-start);
			//	Check if specific repository
			start = strloc.find("/cvsroot/");
			if (start!=string::npos && start>0)
			{
				string	repository = strloc.substr(0, start);
				if (repository.find("/segfs/")!=string::npos)
					strloc = "ESRF:" + strloc.substr(start, strloc.length()-start);
			}
			Tango::DbDatum	cvs_loc("cvs_location");
			cvs_loc << strloc;
			data.push_back(cvs_loc);
		}
	}

	// check for svn information
	else
	{
		string	src_path(SvnPath);
		start = src_path.find("://");
		if (start!=string::npos)
		{
			end = src_path.find(filename);
			if (end>start)
			{
				header = "$HeadURL: ";
				start = header.length();
				string	strloc = src_path.substr(start, (end-start));
				
				Tango::DbDatum	svn_loc("svn_location");
				svn_loc << strloc;
				data.push_back(svn_loc);
			}
		}
	}

	//	Get CVS or SVN revision tag
	
	// CVS tag
	string	tagname(TagName);
	header = "$Name: ";
	start = header.length();
	string	endstr(" $");
	
	end   = tagname.find(endstr);
	if (end!=string::npos && end>start)
	{
		string	strtag = tagname.substr(start, end-start);
		Tango::DbDatum	cvs_tag("cvs_tag");
		cvs_tag << strtag;
		data.push_back(cvs_tag);
	}
	
	// SVN tag
	string	svnpath(SvnPath);
	header = "$HeadURL: ";
	start = header.length();
	
	end   = svnpath.find(endstr);
	if (end!=string::npos && end>start)
	{
		string	strloc = svnpath.substr(start, end-start);
		
		string tagstr ("/tags/");
		start = strloc.find(tagstr);
		if ( start!=string::npos )
		{
			start = start + tagstr.length();
			end   = strloc.find(filename);
			string	strtag = strloc.substr(start, end-start-1);
			
			Tango::DbDatum	svn_tag("svn_tag");
			svn_tag << strtag;
			data.push_back(svn_tag);
		}
	}

	//	Get URL location
	string	httpServ(HttpServer);
	if (httpServ.length()>0)
	{
		Tango::DbDatum	db_doc_url("doc_url");
		db_doc_url << httpServ;
		data.push_back(db_doc_url);
	}

	//  Put inheritance
	Tango::DbDatum	inher_datum("InheritedFrom");
	vector<string> inheritance;
	inheritance.push_back("TANGO_BASE_CLASS");
	inher_datum << inheritance;
	data.push_back(inher_datum);

	//	Call database and and values
	get_db_class()->put_property(data);
}

//===================================================================
//	Factory methods
//===================================================================

//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::device_factory()
 *	Description : Create the device object(s)
 *                and store them in the device list
 */
//--------------------------------------------------------
void SlsDetectorClass::device_factory(const Tango::DevVarStringArray *devlist_ptr)
{
	/*----- PROTECTED REGION ID(SlsDetectorClass::device_factory_before) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::device_factory_before

	//	Create devices and add it into the device list
	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{
		cout4 << "Device name : " << (*devlist_ptr)[i].in() << endl;
		device_list.push_back(new SlsDetector(this, (*devlist_ptr)[i]));							 
	}

	//	Manage dynamic attributes if any
	erase_dynamic_attributes(devlist_ptr, get_class_attr()->get_attr_list());

	//	Export devices to the outside world
	for (unsigned long i=1 ; i<=devlist_ptr->length() ; i++)
	{
		//	Add dynamic attributes if any
		SlsDetector *dev = static_cast<SlsDetector *>(device_list[device_list.size()-i]);
		dev->add_dynamic_attributes();

		//	Check before if database used.
		if ((Tango::Util::_UseDb == true) && (Tango::Util::_FileDb == false))
			export_device(dev);
		else
			export_device(dev, dev->get_name().c_str());
	}

	/*----- PROTECTED REGION ID(SlsDetectorClass::device_factory_after) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::device_factory_after
}
//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::attribute_factory()
 *	Description : Create the attribute object(s)
 *                and store them in the attribute list
 */
//--------------------------------------------------------
void SlsDetectorClass::attribute_factory(vector<Tango::Attr *> &att_list)
{
	/*----- PROTECTED REGION ID(SlsDetectorClass::attribute_factory_before) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::attribute_factory_before
	//	Attribute : thresholdEnergy
	thresholdEnergyAttrib	*thresholdenergy = new thresholdEnergyAttrib();
	Tango::UserDefaultAttrProp	thresholdenergy_prop;
	//	description	not set for thresholdEnergy
	//	label	not set for thresholdEnergy
	//	unit	not set for thresholdEnergy
	//	standard_unit	not set for thresholdEnergy
	thresholdenergy_prop.set_display_unit(" ");
	//	format	not set for thresholdEnergy
	//	max_value	not set for thresholdEnergy
	//	min_value	not set for thresholdEnergy
	//	max_alarm	not set for thresholdEnergy
	//	min_alarm	not set for thresholdEnergy
	//	max_warning	not set for thresholdEnergy
	//	min_warning	not set for thresholdEnergy
	//	delta_t	not set for thresholdEnergy
	//	delta_val	not set for thresholdEnergy
	
	thresholdenergy->set_default_properties(thresholdenergy_prop);
	//	Not Polled
	thresholdenergy->set_disp_level(Tango::OPERATOR);
	thresholdenergy->set_memorized();
	thresholdenergy->set_memorized_init(false);
	att_list.push_back(thresholdenergy);

	//	Attribute : clockDivider
	clockDividerAttrib	*clockdivider = new clockDividerAttrib();
	Tango::UserDefaultAttrProp	clockdivider_prop;
	clockdivider_prop.set_description("Changes the readout clock.<br>\nAvailable clock Divider :<br>\nFULL_SPEED<br>\nHALF_SPEED<br>\nQUARTER_SPEED<br>\nSUPER_SLOW_SPEED<br>");
	//	label	not set for clockDivider
	//	unit	not set for clockDivider
	//	standard_unit	not set for clockDivider
	clockdivider_prop.set_display_unit(" ");
	//	format	not set for clockDivider
	//	max_value	not set for clockDivider
	//	min_value	not set for clockDivider
	//	max_alarm	not set for clockDivider
	//	min_alarm	not set for clockDivider
	//	max_warning	not set for clockDivider
	//	min_warning	not set for clockDivider
	//	delta_t	not set for clockDivider
	//	delta_val	not set for clockDivider
	
	clockdivider->set_default_properties(clockdivider_prop);
	//	Not Polled
	clockdivider->set_disp_level(Tango::OPERATOR);
	clockdivider->set_memorized();
	clockdivider->set_memorized_init(true);
	att_list.push_back(clockdivider);

	//	Attribute : configFileName
	configFileNameAttrib	*configfilename = new configFileNameAttrib();
	Tango::UserDefaultAttrProp	configfilename_prop;
	//	description	not set for configFileName
	//	label	not set for configFileName
	//	unit	not set for configFileName
	//	standard_unit	not set for configFileName
	configfilename_prop.set_display_unit(" ");
	//	format	not set for configFileName
	//	max_value	not set for configFileName
	//	min_value	not set for configFileName
	//	max_alarm	not set for configFileName
	//	min_alarm	not set for configFileName
	//	max_warning	not set for configFileName
	//	min_warning	not set for configFileName
	//	delta_t	not set for configFileName
	//	delta_val	not set for configFileName
	
	configfilename->set_default_properties(configfilename_prop);
	//	Not Polled
	configfilename->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(configfilename);

	//	Attribute : rawMode
	rawModeAttrib	*rawmode = new rawModeAttrib();
	Tango::UserDefaultAttrProp	rawmode_prop;
	//	description	not set for rawMode
	//	label	not set for rawMode
	//	unit	not set for rawMode
	//	standard_unit	not set for rawMode
	rawmode_prop.set_display_unit(" ");
	//	format	not set for rawMode
	//	max_value	not set for rawMode
	//	min_value	not set for rawMode
	//	max_alarm	not set for rawMode
	//	min_alarm	not set for rawMode
	//	max_warning	not set for rawMode
	//	min_warning	not set for rawMode
	//	delta_t	not set for rawMode
	//	delta_val	not set for rawMode
	
	rawmode->set_default_properties(rawmode_prop);
	//	Not Polled
	rawmode->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(rawmode);

	//	Attribute : readoutFlags
	readoutFlagsAttrib	*readoutflags = new readoutFlagsAttrib();
	Tango::UserDefaultAttrProp	readoutflags_prop;
	//	description	not set for readoutFlags
	//	label	not set for readoutFlags
	//	unit	not set for readoutFlags
	//	standard_unit	not set for readoutFlags
	readoutflags_prop.set_display_unit(" ");
	//	format	not set for readoutFlags
	//	max_value	not set for readoutFlags
	//	min_value	not set for readoutFlags
	//	max_alarm	not set for readoutFlags
	//	min_alarm	not set for readoutFlags
	//	max_warning	not set for readoutFlags
	//	min_warning	not set for readoutFlags
	//	delta_t	not set for readoutFlags
	//	delta_val	not set for readoutFlags
	
	readoutflags->set_default_properties(readoutflags_prop);
	//	Not Polled
	readoutflags->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(readoutflags);

	//	Attribute : tolerateLostPackets
	tolerateLostPacketsAttrib	*toleratelostpackets = new tolerateLostPacketsAttrib();
	Tango::UserDefaultAttrProp	toleratelostpackets_prop;
	//	description	not set for tolerateLostPackets
	//	label	not set for tolerateLostPackets
	//	unit	not set for tolerateLostPackets
	//	standard_unit	not set for tolerateLostPackets
	toleratelostpackets_prop.set_display_unit(" ");
	//	format	not set for tolerateLostPackets
	//	max_value	not set for tolerateLostPackets
	//	min_value	not set for tolerateLostPackets
	//	max_alarm	not set for tolerateLostPackets
	//	min_alarm	not set for tolerateLostPackets
	//	max_warning	not set for tolerateLostPackets
	//	min_warning	not set for tolerateLostPackets
	//	delta_t	not set for tolerateLostPackets
	//	delta_val	not set for tolerateLostPackets
	
	toleratelostpackets->set_default_properties(toleratelostpackets_prop);
	//	Not Polled
	toleratelostpackets->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(toleratelostpackets);

	//	Attribute : nbBadFrames
	nbBadFramesAttrib	*nbbadframes = new nbBadFramesAttrib();
	Tango::UserDefaultAttrProp	nbbadframes_prop;
	//	description	not set for nbBadFrames
	//	label	not set for nbBadFrames
	//	unit	not set for nbBadFrames
	//	standard_unit	not set for nbBadFrames
	nbbadframes_prop.set_display_unit(" ");
	//	format	not set for nbBadFrames
	//	max_value	not set for nbBadFrames
	//	min_value	not set for nbBadFrames
	//	max_alarm	not set for nbBadFrames
	//	min_alarm	not set for nbBadFrames
	//	max_warning	not set for nbBadFrames
	//	min_warning	not set for nbBadFrames
	//	delta_t	not set for nbBadFrames
	//	delta_val	not set for nbBadFrames
	
	nbbadframes->set_default_properties(nbbadframes_prop);
	//	Not Polled
	nbbadframes->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(nbbadframes);

	//	Attribute : hostnameList
	hostnameListAttrib	*hostnamelist = new hostnameListAttrib();
	Tango::UserDefaultAttrProp	hostnamelist_prop;
	//	description	not set for hostnameList
	//	label	not set for hostnameList
	//	unit	not set for hostnameList
	//	standard_unit	not set for hostnameList
	//	display_unit	not set for hostnameList
	//	format	not set for hostnameList
	//	max_value	not set for hostnameList
	//	min_value	not set for hostnameList
	//	max_alarm	not set for hostnameList
	//	min_alarm	not set for hostnameList
	//	max_warning	not set for hostnameList
	//	min_warning	not set for hostnameList
	//	delta_t	not set for hostnameList
	//	delta_val	not set for hostnameList
	
	hostnamelist->set_default_properties(hostnamelist_prop);
	//	Not Polled
	hostnamelist->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(hostnamelist);

	//	Attribute : dacNameList
	dacNameListAttrib	*dacnamelist = new dacNameListAttrib();
	Tango::UserDefaultAttrProp	dacnamelist_prop;
	//	description	not set for dacNameList
	//	label	not set for dacNameList
	//	unit	not set for dacNameList
	//	standard_unit	not set for dacNameList
	//	display_unit	not set for dacNameList
	//	format	not set for dacNameList
	//	max_value	not set for dacNameList
	//	min_value	not set for dacNameList
	//	max_alarm	not set for dacNameList
	//	min_alarm	not set for dacNameList
	//	max_warning	not set for dacNameList
	//	min_warning	not set for dacNameList
	//	delta_t	not set for dacNameList
	//	delta_val	not set for dacNameList
	
	dacnamelist->set_default_properties(dacnamelist_prop);
	//	Not Polled
	dacnamelist->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(dacnamelist);

	//	Attribute : dacNameListMv
	dacNameListMvAttrib	*dacnamelistmv = new dacNameListMvAttrib();
	Tango::UserDefaultAttrProp	dacnamelistmv_prop;
	//	description	not set for dacNameListMv
	//	label	not set for dacNameListMv
	//	unit	not set for dacNameListMv
	//	standard_unit	not set for dacNameListMv
	//	display_unit	not set for dacNameListMv
	//	format	not set for dacNameListMv
	//	max_value	not set for dacNameListMv
	//	min_value	not set for dacNameListMv
	//	max_alarm	not set for dacNameListMv
	//	min_alarm	not set for dacNameListMv
	//	max_warning	not set for dacNameListMv
	//	min_warning	not set for dacNameListMv
	//	delta_t	not set for dacNameListMv
	//	delta_val	not set for dacNameListMv
	
	dacnamelistmv->set_default_properties(dacnamelistmv_prop);
	//	Not Polled
	dacnamelistmv->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(dacnamelistmv);

	//	Attribute : adcNameList
	adcNameListAttrib	*adcnamelist = new adcNameListAttrib();
	Tango::UserDefaultAttrProp	adcnamelist_prop;
	//	description	not set for adcNameList
	//	label	not set for adcNameList
	//	unit	not set for adcNameList
	//	standard_unit	not set for adcNameList
	//	display_unit	not set for adcNameList
	//	format	not set for adcNameList
	//	max_value	not set for adcNameList
	//	min_value	not set for adcNameList
	//	max_alarm	not set for adcNameList
	//	min_alarm	not set for adcNameList
	//	max_warning	not set for adcNameList
	//	min_warning	not set for adcNameList
	//	delta_t	not set for adcNameList
	//	delta_val	not set for adcNameList
	
	adcnamelist->set_default_properties(adcnamelist_prop);
	//	Not Polled
	adcnamelist->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(adcnamelist);

	//	Attribute : allTrimBits
	allTrimBitsAttrib	*alltrimbits = new allTrimBitsAttrib();
	Tango::UserDefaultAttrProp	alltrimbits_prop;
	//	description	not set for allTrimBits
	//	label	not set for allTrimBits
	//	unit	not set for allTrimBits
	//	standard_unit	not set for allTrimBits
	//	display_unit	not set for allTrimBits
	//	format	not set for allTrimBits
	//	max_value	not set for allTrimBits
	//	min_value	not set for allTrimBits
	//	max_alarm	not set for allTrimBits
	//	min_alarm	not set for allTrimBits
	//	max_warning	not set for allTrimBits
	//	min_warning	not set for allTrimBits
	//	delta_t	not set for allTrimBits
	//	delta_val	not set for allTrimBits
	
	alltrimbits->set_default_properties(alltrimbits_prop);
	//	Not Polled
	alltrimbits->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(alltrimbits);

	//	Attribute : badFrameList
	badFrameListAttrib	*badframelist = new badFrameListAttrib();
	Tango::UserDefaultAttrProp	badframelist_prop;
	//	description	not set for badFrameList
	//	label	not set for badFrameList
	//	unit	not set for badFrameList
	//	standard_unit	not set for badFrameList
	//	display_unit	not set for badFrameList
	//	format	not set for badFrameList
	//	max_value	not set for badFrameList
	//	min_value	not set for badFrameList
	//	max_alarm	not set for badFrameList
	//	min_alarm	not set for badFrameList
	//	max_warning	not set for badFrameList
	//	min_warning	not set for badFrameList
	//	delta_t	not set for badFrameList
	//	delta_val	not set for badFrameList
	
	badframelist->set_default_properties(badframelist_prop);
	//	Not Polled
	badframelist->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(badframelist);

	//	Create a list of static attributes
	create_static_attribute_list(get_class_attr()->get_attr_list());
	/*----- PROTECTED REGION ID(SlsDetectorClass::attribute_factory_after) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::attribute_factory_after
}
//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::command_factory()
 *	Description : Create the command object(s)
 *                and store them in the command list
 */
//--------------------------------------------------------
void SlsDetectorClass::command_factory()
{
	/*----- PROTECTED REGION ID(SlsDetectorClass::command_factory_before) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::command_factory_before


	//	Command SetCmd
	SetCmdClass	*pSetCmdCmd =
		new SetCmdClass("SetCmd",
			Tango::DEV_STRING, Tango::DEV_VOID,
			"SlsDetector command",
			"",
			Tango::OPERATOR);
	command_list.push_back(pSetCmdCmd);

	//	Command GetCmd
	GetCmdClass	*pGetCmdCmd =
		new GetCmdClass("GetCmd",
			Tango::DEV_STRING, Tango::DEV_STRING,
			"SlsDetector command",
			"SlsDetector response",
			Tango::OPERATOR);
	command_list.push_back(pGetCmdCmd);

	/*----- PROTECTED REGION ID(SlsDetectorClass::command_factory_after) ENABLED START -----*/
	
	//	Add your own code
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::command_factory_after
}

//===================================================================
//	Dynamic attributes related methods
//===================================================================

//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::create_static_attribute_list
 * description : 	Create the a list of static attributes
 *
 * @param	att_list	the ceated attribute list 
 */
//--------------------------------------------------------
void SlsDetectorClass::create_static_attribute_list(vector<Tango::Attr *> &att_list)
{
	for (unsigned long i=0 ; i<att_list.size() ; i++)
	{
		string att_name(att_list[i]->get_name());
		transform(att_name.begin(), att_name.end(), att_name.begin(), ::tolower);
		defaultAttList.push_back(att_name);
	}

	cout2 << defaultAttList.size() << " attributes in default list" << endl;

	/*----- PROTECTED REGION ID(SlsDetectorClass::create_static_att_list) ENABLED START -----*/
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::create_static_att_list
}


//--------------------------------------------------------
/**
 * method : 		SlsDetectorClass::erase_dynamic_attributes
 * description : 	delete the dynamic attributes if any.
 *
 * @param	devlist_ptr	the device list pointer
 * @param	list of all attributes
 */
//--------------------------------------------------------
void SlsDetectorClass::erase_dynamic_attributes(const Tango::DevVarStringArray *devlist_ptr, vector<Tango::Attr *> &att_list)
{
	Tango::Util *tg = Tango::Util::instance();

	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{	
		Tango::DeviceImpl *dev_impl = tg->get_device_by_name(((string)(*devlist_ptr)[i]).c_str());
		SlsDetector *dev = static_cast<SlsDetector *> (dev_impl);
		
		vector<Tango::Attribute *> &dev_att_list = dev->get_device_attr()->get_attribute_list();
		vector<Tango::Attribute *>::iterator ite_att;
		for (ite_att=dev_att_list.begin() ; ite_att != dev_att_list.end() ; ++ite_att)
		{
			string att_name((*ite_att)->get_name_lower());
			if ((att_name == "state") || (att_name == "status"))
				continue;
			vector<string>::iterator ite_str = find(defaultAttList.begin(), defaultAttList.end(), att_name);
			if (ite_str == defaultAttList.end())
			{
				cout2 << att_name << " is a UNWANTED dynamic attribute for device " << (*devlist_ptr)[i] << endl;
				Tango::Attribute &att = dev->get_device_attr()->get_attr_by_name(att_name.c_str());
				dev->remove_attribute(att_list[att.get_attr_idx()], true, false);
				--ite_att;
			}
		}
	}
	/*----- PROTECTED REGION ID(SlsDetectorClass::erase_dynamic_attributes) ENABLED START -----*/
	
	/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::erase_dynamic_attributes
}

//--------------------------------------------------------
/**
 *	Method      : SlsDetectorClass::get_attr_by_name()
 *	Description : returns Tango::Attr * object found by name
 */
//--------------------------------------------------------
Tango::Attr *SlsDetectorClass::get_attr_object_by_name(vector<Tango::Attr *> &att_list, string attname)
{
	vector<Tango::Attr *>::iterator it;
	for (it=att_list.begin() ; it<att_list.end() ; it++)
		if ((*it)->get_name()==attname)
			return (*it);
	//	Attr does not exist
	return NULL;
}


/*----- PROTECTED REGION ID(SlsDetectorClass::Additional Methods) ENABLED START -----*/

/*----- PROTECTED REGION END -----*/	//	SlsDetectorClass::Additional Methods
} //	namespace
